'use strict';

/**
 * category-realisation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::category-realisation.category-realisation');
