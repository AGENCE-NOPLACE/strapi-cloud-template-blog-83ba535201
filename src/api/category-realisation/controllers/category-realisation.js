'use strict';

/**
 * category-realisation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::category-realisation.category-realisation');
