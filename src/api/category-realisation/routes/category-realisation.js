'use strict';

/**
 * category-realisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::category-realisation.category-realisation');
